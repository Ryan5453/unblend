{
  "name": "unblend-workspace",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "unblend",
    "app"
  ],
  "scripts": {
    "build:lib": "npm run build -w unblend",
    "build:app": "npm run build -w unblend-app",
    "build": "npm run build:lib && npm run build:app",
    "test": "npm run test -w unblend",
    "dev": "npm run build:lib && npm run dev -w unblend-app"
  }
}
